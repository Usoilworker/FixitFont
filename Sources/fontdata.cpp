#include "fontdata.h"

FontData::FontData() : characterPatterns() {
    // The default constructor of std::array and std::bitset will initialize all bits to 0.
    // No explicit initialization needed here unless you want a different default state.
}